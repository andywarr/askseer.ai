{
  "name": "db-worker",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "tsx watch src/server.ts",
    "dev:build": "node build.js && node ./build/server.cjs",
    "start": "node ./build/server.cjs",
    "clean": "rm -rf build",
    "type-check": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "script": "tsx scripts/backfill-communication-preferences.ts"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3",
    "@prisma/client": "^6",
    "@types/pino": "^7",
    "cors": "^2",
    "dotenv": "^16",
    "express": "^4",
    "pino": "^9",
    "prisma": "^6"
  },
  "devDependencies": {
    "@types/cors": "^2",
    "@types/express": "^5",
    "@types/node": "^22",
    "esbuild": "^0",
    "tsx": "^4",
    "typescript": "^5"
  },
  "type": "module"
}
