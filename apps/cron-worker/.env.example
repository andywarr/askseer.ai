# target Next.js application URL
NEXTJS_APP_URL=http://localhost:3000

# Cron Secret (Must match Next.js CRON_SECRET)
CRON_SECRET=your-cron-secret-token

# Cron Schedule expression (optional, defaults to hourly)
CRON_SCHEDULE=0 * * * *
